#include "molecule_visualization.hpp"

// using namespace std;

// //using Molecule object, load in moleculeMapper
// void Visualize::setup_moleculeMapper(){
//     vtkMolecule *mol = vtkMolecule::New();

//     //append atoms to molecule
//    for (int i = 0; i < molecule.num_atoms; i++){
//     mol->AppendAtom(molecule.atoms[i].element_symbol, molecule.atoms[i].center[0], molecule.atoms[i].center[1], molecule.atoms[i].center[2]);
//    }

   

// }